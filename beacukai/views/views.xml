<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="beacukai.list">
      <field name="name">beacukai list</field>
      <field name="model">beacukai.beacukai</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="beacukai.action_window">
      <field name="name">beacukai window</field>
      <field name="res_model">beacukai.beacukai</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="beacukai.action_server">
      <field name="name">beacukai server</field>
      <field name="model_id" ref="model_beacukai_beacukai"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="beacukai" id="beacukai.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="beacukai.menu_1" parent="beacukai.menu_root"/>
    <menuitem name="Menu 2" id="beacukai.menu_2" parent="beacukai.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="beacukai.menu_1_list" parent="beacukai.menu_1"
              action="beacukai.action_window"/>
    <menuitem name="Server to list" id="beacukai" parent="beacukai.menu_2"
              action="beacukai.action_server"/>
    -->
  </data>
</odoo>